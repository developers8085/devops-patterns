version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
    - echo "Iam reaching here"
  build:
    commands:
    - ls
    - chmod +x scripts/role-codebuild/deploy.sh
    - scripts/role-codebuild/deploy.sh